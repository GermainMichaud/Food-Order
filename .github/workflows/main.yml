name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Nrwl Nx
        uses: MansaGroup/nrwl-nx-action@v2
        with:
          targets: lint, test, build

